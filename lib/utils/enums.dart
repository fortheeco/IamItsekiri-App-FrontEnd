enum InputType { email, phone, name, file, password }

enum ApiType {
  signUp,
  signIn,
  logOut,
  requestSignUpOtp,
  verifyOtp,
  getUserData,
  changePassword,
  updateUserProfile
}

enum HttpMethod { post, patch, get }
